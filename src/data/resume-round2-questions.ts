import { Question } from './types';

export const resumeRound2Questions: Question[] = [
  {
    id: 4001,
    category: '二面深度',
    difficulty: 'medium',
    question: '如果让你从零设计一个 Server Driven UI 框架，你会怎么设计协议？怎么处理模块间通信？',
    answer: `SDUI 框架设计要点：

协议设计（三层结构）：
{
  "layout": [
    { "moduleKey": "header", "priority": 0, "config": { "height": 300 } },
    { "moduleKey": "price", "priority": 0 },
    { "moduleKey": "sku", "priority": 1 }
  ],
  "response": {
    "header": { "imageUrl": "...", "title": "..." },
    "price": { "originalPrice": 99, "salePrice": 69 },
    "sku": { "list": [...] }
  },
  "actions": {
    "sku_change": { "target": ["price", "bottomBar"], "type": "refresh" }
  }
}

三层含义：
- layout: 页面结构（顺序、优先级、配置）
- response: 各模块数据
- actions: 模块间联动规则（服务端可控）

模块间通信方案：

方案一：事件总线
const eventBus = new EventEmitter();
// SKU 模块发布事件
eventBus.emit('sku_change', { skuId: '123' });
// 价格模块订阅
eventBus.on('sku_change', (data) => fetchPrice(data.skuId));

方案二：共享 Store
// 全局 store 存放跨模块共享状态
const sharedStore = {
    selectedSkuId: observable,
    currentPrice: computed
};

方案三：服务端声明式（推荐）
// actions 协议中声明联动关系
// 框架自动处理：A模块变化 -> 通知B模块刷新
// 好处：联动逻辑可服务端动态配置

美团实际选择：方案二（MobX Store）+ 方案三（actions 协议）结合`,
    oralAnswer: `如果从零设计 SDUI 框架，我会把协议分三层。

第一层 layout 描述页面结构：哪些模块、什么顺序、优先级和配置。第二层 response 是各模块的数据。第三层 actions 定义模块间联动规则，比如 SKU 变化要通知价格和底栏刷新。这三层分离的好处是服务端可以动态调整页面结构和联动逻辑，前端不需要发版。

模块间通信方案有三种。事件总线：松耦合但难追踪。共享 Store：MobX observable 管理跨模块状态，可追踪。服务端声明式：actions 协议中配置联动关系，框架自动处理，好处是联动逻辑可服务端动态配置。

美团实际选择是 MobX Store 加 actions 协议结合。Store 管理实时状态，actions 描述联动规则。`,
  },
  {
    id: 4002,
    category: '二面深度',
    difficulty: 'medium',
    question: '你做的首屏优化方案有没有 trade-off？数据缓存会不会导致用户看到过期信息？怎么平衡？',
    answer: `MRNBox 首屏快照的 trade-off 分析：

收益：
- 首屏时间从 800ms -> 200ms
- 用户感知秒开，体验显著提升

代价与风险：
1. 数据新鲜度问题：
   - 价格可能过期（商家改价了）
   - 库存可能不准（已售罄但缓存显示有货）
   - 活动状态变化（活动结束但缓存还显示优惠价）

2. 视觉跳变：
   - 缓存数据渲染 -> 真实数据到达 -> 界面更新
   - 如果差异大，用户看到价格「跳变」

平衡策略：

1. 分模块缓存策略：
   - 头图/标题：强缓存（几乎不变）
   - 价格/库存：弱缓存（展示时标记"价格加载中..."）
   - 活动信息：不缓存（每次请求）

2. 快速覆盖机制：
   - 真实数据 RT 控制在 200ms 内
   - 缓存数据最多展示 200ms 就被覆盖
   - 用户几乎感知不到差异

3. 缓存过期策略：
   - TTL 控制：缓存超过 24h 不使用
   - 版本号：商品有编辑操作时失效缓存
   - 兜底：缓存数据加"仅供参考"标识

4. 视觉一致性处理：
   - 价格变化用数字滚动动画过渡
   - 避免突然跳变的突兀感
   - 差异过大时用骨架屏过渡

5. 监控机制：
   - 统计缓存命中率和数据偏差率
   - 偏差超过阈值告警
   - AB 实验验证对转化的影响

总结：在「速度」和「准确性」之间，选择了「先快后准」的策略。`,
    oralAnswer: `MRNBox 快照最大的 trade-off 就是速度和数据新鲜度的矛盾。收益是首屏从 800ms 优化到 200ms 用户感知秒开。代价是缓存数据可能过期，价格可能改了、库存可能卖完了、活动可能结束了。

我们的平衡策略是分模块差异化处理。头图标题几乎不变就强缓存。价格库存变化频繁就弱缓存，展示时标记"价格加载中"。活动信息每次都请求不缓存。

关键机制是真实数据 200ms 内就回来了，缓存最多展示 200ms 就被覆盖掉，用户几乎感知不到差异。如果价格变化大就用数字滚动动画过渡避免突兀跳变。缓存超过 24 小时不使用。再加监控统计缓存命中率和数据偏差率，偏差超阈值告警。

总结就是「先快后准」——先让用户看到内容，200ms 后用真实数据静默替换。`,
  },
  {
    id: 4003,
    category: '二面深度',
    difficulty: 'medium',
    question: '多仓架构中，模块 A 依赖模块 B 的某个能力，但 B 还没发版，你怎么处理联调？',
    answer: `多仓联调的几种方案：

方案一：npm link / yalc（本地联调）
# 在模块 B 仓库
yalc publish
# 在装配仓
yalc add @mrn/module-b
# 好处：实时生效，改了 B 立即反映到装配仓

方案二：发布预发版本
npm publish --tag beta  # @mrn/module-b@1.2.0-beta.1
# 装配仓安装 beta 版本联调
# 好处：CI 能跑通，更接近真实环境

方案三：monorepo workspace（如果条件允许）
# pnpm workspace 统一管理
# 本地直接引用源码
# 好处：改动即生效，无需发版

方案四：Mock 接口 + 协议先行
# 先约定 B 模块的输入输出协议（TypeScript interface）
# A 模块根据协议开发，用 mock 数据验证
# B 模块完成后替换即可

美团实际做法：
1. 开发阶段：yalc + 本地 link
2. 联调阶段：发 beta 版本到内部 npm
3. 集成测试：装配仓统一升级到正式版
4. 紧急情况：直接在装配仓临时 patch

注意事项：
- lockfile 管理：beta 版本不能进入主分支 lockfile
- 版本号规范：beta/rc/release 严格区分
- CI 卡点：主分支不允许依赖 beta 版本`,
    oralAnswer: `多仓联调有四种方案。

第一种 yalc 本地联调：在模块 B 仓执行 yalc publish，在装配仓 yalc add，改了 B 立即反映到装配仓，适合开发阶段快速迭代。第二种发 beta 版本：npm publish --tag beta 发预发版本，装配仓安装联调，好处是 CI 能跑通更接近真实环境。第三种 monorepo workspace：如果条件允许用 pnpm workspace 统一管理直接引用源码。第四种协议先行：先约定模块 B 的 TypeScript interface，A 模块根据协议用 mock 数据开发，B 完成后替换。

美团实际做法是：开发阶段 yalc 本地 link，联调阶段发 beta 到内部 npm，集成测试统一升级正式版。注意 beta 版本不能进主分支 lockfile，CI 有卡点不允许主分支依赖 beta。`,
  },
  {
    id: 4004,
    category: '二面深度',
    difficulty: 'medium',
    question: '你提到 AB 实验通过 SDUI 控制模块显隐，如果某个实验导致页面白屏了，你会怎么排查和应急？',
    answer: `白屏应急和排查流程：

第一步：应急止血（5分钟内）
1. 确认影响范围（哪些用户/设备/地区）
2. 如果是 AB 实验导致 → 立即关闭实验（回到对照组）
3. 如果是全量 → 回滚上一版本 BFF 配置
4. 通知相关方（PM/QA/后端）

第二步：问题定位
1. 看监控大盘：
   - JS Error 是否飙升？
   - BFF 接口是否异常？
   - 是特定设备/系统版本？

2. 复现路径：
   - 获取出问题的 AB 实验分组 ID
   - 本地模拟相同分组请求 BFF
   - 查看返回数据是否异常

3. 常见白屏原因：
   a) 实验组下发了未注册的 moduleKey
      → 前端找不到对应组件，整个页面渲染失败
   b) 实验组移除了关键模块（如容器模块）
      → 子模块失去挂载点
   c) 模块数据结构变化
      → 实验组返回了新字段格式，旧版本不兼容

4. 验证修复：
   - 灰度环境验证
   - 逐步放量

第三步：复盘和预防
1. 前端防御：
   - moduleKey 未注册时 fallback 到空 View（不白屏）
   - ErrorBoundary 粒度到模块级
   - layout 为空时展示降级页面

2. BFF 防御：
   - 实验配置必须经过模板校验
   - 不允许删除 P0 模块
   - 变更需要 Review + 灰度

3. 监控防御：
   - 白屏率监控 + 自动告警
   - 实验开启后 1 小时内强化监控
   - 异常自动触发实验暂停`,
    oralAnswer: `AB 实验导致白屏我会分三步处理。

第一步应急止血 5 分钟内完成：确认影响范围，如果是 AB 实验导致就立即关闭实验让所有用户回到对照组，如果是全量问题就回滚 BFF 配置，同时通知 PM 和后端。

第二步问题定位：获取出问题的实验分组 ID，本地模拟相同分组请求 BFF 看返回数据。常见白屏原因有三个——实验组下发了前端未注册的 moduleKey 导致找不到组件整体渲染失败，实验组移除了关键容器模块导致子模块没有挂载点，或者模块数据结构变化新格式旧版本不兼容。

第三步复盘预防：前端加防御——moduleKey 未注册 fallback 到空 View 不白屏，ErrorBoundary 粒度细化到模块级，layout 为空展示降级页面。BFF 侧实验配置必须模板校验不允许删除 P0 模块。监控侧白屏率告警加实验开启后 1 小时强化监控，异常自动暂停实验。`,
  },
  {
    id: 4005,
    category: '二面深度',
    difficulty: 'hard',
    question: '如果让你设计一个大型 RN 应用的性能监控体系，从哪些维度来衡量？怎么做自动告警？',
    answer: `性能监控体系设计：

核心指标维度：

1. 页面性能：
   - 首屏时间（FMP/LCP）：用户看到有意义内容的时间
   - 可交互时间（TTI）：页面响应用户操作的时间
   - 白屏率：超过 3s 未渲染的比例

2. 运行时性能：
   - JS 帧率：JS 线程 FPS（目标 60fps）
   - UI 帧率：Native UI 线程 FPS
   - 内存使用：JS Heap + Native 内存

3. 稳定性：
   - JS Error 率（Crash 率）
   - Native Crash 率
   - ANR（Application Not Responding）率

4. 网络性能：
   - BFF 接口 RT（P50/P90/P99）
   - 请求成功率
   - 超时率

5. 业务指标：
   - 页面加载完成率
   - 模块渲染成功率
   - 埋点上报成功率

数据采集方案：
// 首屏时间采集
const startTime = NativeModule.getPageOpenTime(); // Native 记录
const endTime = performance.now(); // 首屏模块 onLayout 后记录
report({ metric: 'fmp', value: endTime - startTime, page: 'detail' });

// 帧率采集
const frameCallback = (frameInfo) => {
    if (frameInfo.droppedFrames > 0) {
        report({ metric: 'jank', dropped: frameInfo.droppedFrames });
    }
};

自动告警策略：
1. 阈值告警：
   - 首屏 P90 > 2s → P1 告警
   - JS Error 率 > 0.5% → P0 告警
   - 白屏率 > 1% → P0 告警

2. 环比告警：
   - 较昨日同时段劣化 > 20% → 告警
   - 发版后 1 小时内持续劣化 → 自动暂停灰度

3. 分维度告警：
   - 按设备等级：低端机指标单独阈值
   - 按网络类型：弱网指标单独阈值
   - 按版本号：新版本单独监控

落地工具：美团 CAT + 自建前端监控 + Grafana 看板`,
    oralAnswer: `性能监控体系我从五个维度设计。

页面性能：首屏时间 FMP、可交互时间 TTI、白屏率。运行时性能：JS 帧率和 UI 帧率目标 60fps、内存使用。稳定性：JS Error 率、Native Crash 率、ANR 率。网络性能：BFF 接口 RT 的 P50/P90/P99、请求成功率、超时率。业务指标：页面加载完成率、模块渲染成功率。

数据采集方面首屏时间用 Native 记录页面打开时间作为起点，首屏模块 onLayout 后作为终点。帧率用 frameCallback 检测掉帧。

告警策略分三类。阈值告警：首屏 P90 超 2 秒 P1 告警，JS Error 率超 0.5% P0 告警。环比告警：较昨日同时段劣化超 20% 告警，发版后持续劣化自动暂停灰度。分维度告警：低端机和弱网单独设阈值，新版本单独监控。

落地用美团 CAT 加自建前端监控加 Grafana 看板。`,
  },
  {
    id: 4006,
    category: '二面深度',
    difficulty: 'hard',
    question: '你提到 AI Coding 工程化，如果 AI 生成的代码引入了 Bug 到线上，责任怎么划分？你们怎么防护？',
    answer: `AI 生成代码的质量防护体系：

核心原则：AI 是工具，人是最终负责人。
AI 生成的代码 = 人写的代码，一样要经过完整的质量门禁。

防护层级：

1. 生成阶段（AI 侧）：
   - rules 约束：AI 必须遵循编码规范
   - 上下文注入：提供完整的类型定义和接口文档
   - 限制范围：AI 只生成模块内部代码，不动全局配置

2. Review 阶段（人工 + AI）：
   - 自动 Review：AI 对自己生成的代码做 self-review
   - 人工 Review：开发者必须逐行审查 AI 输出
   - Checklist：特别关注边界条件、类型安全、异步处理

3. 测试阶段：
   - 单元测试：AI 同时生成对应测试用例
   - 集成测试：在装配仓跑全量回归
   - 视觉对比：截图对比 MRN 和小程序一致性

4. 发布阶段：
   - 灰度发布：新代码先 5% 灰度
   - 监控强化：AI 生成的模块标记 tag，单独监控
   - 快速回滚：问题 5 分钟内回滚

责任划分：
- AI 生成 → 开发者 Review 通过 → 开发者负责
- 如果 Review 遗漏 → 复盘 Review 机制
- 如果是 AI 框架缺陷（如 rules 不完善）→ 平台团队优化

实际案例：
AI 在 MRN→小程序迁移中生成了不兼容的 CSS 属性
→ 自动化视觉对比测试捕获
→ 添加 rule："Skyline 不支持 backdrop-filter"
→ 后续自动避免`,
    oralAnswer: `核心原则是 AI 是工具人是最终负责人，AI 生成的代码和人写的一样要经过完整质量门禁。

防护分四层。生成阶段：rules 约束 AI 遵循编码规范，注入完整的类型定义和接口文档作为上下文，限制 AI 只生成模块内部代码不动全局配置。Review 阶段：AI 先做 self-review，然后开发者必须逐行审查，特别关注边界条件、类型安全和异步处理。测试阶段：AI 同时生成单元测试，在装配仓跑集成测试，再做视觉截图对比。发布阶段：5% 灰度起步，AI 生成的模块打 tag 单独监控，问题 5 分钟内回滚。

责任划分很清晰：AI 生成代码经开发者 Review 通过后由开发者负责。如果 Review 遗漏就复盘 Review 机制。如果是 AI 框架缺陷比如 rules 不完善就由平台团队优化。实际案例是 AI 迁移时生成了不兼容的 CSS 属性，被视觉对比测试捕获，添加 rule 后自动避免。`,
  },
  {
    id: 4007,
    category: '二面深度',
    difficulty: 'hard',
    question: '如果美团详情页要支持一个新端（比如鸿蒙），你会怎么评估工作量？架构上需要做哪些适配？',
    answer: `新端接入评估框架：

工作量评估维度：

1. 渲染层适配（最大工作量 60%）：
   - 基础组件映射：RN View/Text/Image → ArkUI 对应组件
   - 布局系统：Flexbox → ArkUI Flex/Column/Row
   - 动画系统：Animated → ArkUI 动画 API
   - 手势系统：Gesture Handler → ArkUI 手势
   预估：20+ 基础组件，每个 2-3 天 = 40-60 人天

2. Bridge/通信层（20%）：
   - JS 引擎集成（Hermes/JSI 在鸿蒙的支持）
   - Native Module 适配
   - 图片加载库
   预估：15-20 人天

3. 业务模块适配（15%）：
   - 如果 SDUI 架构做得好，数据层无需改动
   - 只需处理平台特有的 UI 差异
   - 20+ 模块各 1-2 天 = 20-40 人天

4. 工程化（5%）：
   - 构建系统适配
   - CI/CD 流水线
   - 测试环境搭建
   预估：5-10 人天

架构适配策略：

1. 抽象层设计：
   // 定义跨端接口
   interface PlatformBridge {
       getStatusBarHeight(): number;
       openUrl(url: string): void;
       getDeviceInfo(): DeviceInfo;
   }
   // 各端实现
   class HarmonyBridge implements PlatformBridge { ... }

2. SDUI 的优势：
   - 数据层（BFF）完全复用，无需改动
   - layout 协议不变
   - 只需在新端实现 moduleKey → ArkUI Component 映射

3. 降级策略：
   - 初期：复杂模块用 WebView 嵌套降级
   - 中期：核心模块原生实现
   - 长期：全量原生渲染

4. 验收标准：
   - 视觉一致性：与 MRN 首屏对比 95%+ 像素一致
   - 性能达标：首屏时间不劣于 MRN
   - 功能完整：P0 功能 100% 覆盖`,
    oralAnswer: `评估新端接入我会从四个维度拆分工作量。

渲染层适配占 60% 是最大的头，需要把 RN 的 View、Text、Image 等 20 多个基础组件映射到 ArkUI 对应组件，Flexbox 布局映射到 ArkUI 的 Flex/Column/Row，动画和手势系统也要适配，预估 40 到 60 人天。Bridge 通信层占 20%，JS 引擎集成、Native Module 适配和图片加载库，预估 15 到 20 人天。业务模块适配占 15%，因为 SDUI 架构数据层不需要改，只处理平台 UI 差异，20 多个模块各 1 到 2 天。工程化占 5%，构建系统和 CI/CD。

架构策略方面，SDUI 的优势在这里充分体现：BFF 数据层完全复用，layout 协议不变，只需在新端实现 moduleKey 到 ArkUI Component 的映射。降级策略是初期复杂模块用 WebView 降级，中期核心模块原生实现，长期全量原生。验收标准是像素一致性 95% 以上，首屏时间不劣于 MRN。`,
  },
  {
    id: 4008,
    category: '二面深度',
    difficulty: 'hard',
    question: '详情页有 20+ 个模块，模块之间有复杂的依赖关系，你怎么保证修改一个模块不会影响其他模块？',
    answer: `模块隔离和影响面控制：

架构层面保障：
1. 模块独立性原则：
   - 每个模块有明确的输入（Props/Data）和输出（Events）
   - 模块内部状态不暴露给外部
   - 模块间不直接 import，通过事件/Store 通信

2. 数据隔离：
   // 每个模块有独立 store
   class SkuStore { ... }
   class PriceStore { ... }
   // 跨模块通信通过 SharedStore 或事件

3. 接口契约：
   // TypeScript interface 严格定义模块 Props
   interface HeaderModuleProps {
       imageUrl: string;
       title: string;
       tags: string[];
   }
   // Props 变化必须向前兼容

工程层面保障：
1. 模块级 ErrorBoundary：
   // 模块 A 报错不白屏，其他模块正常渲染
   <ErrorBoundary fallback={<ModulePlaceholder />}>
       <SkuModule data={skuData} />
   </ErrorBoundary>

2. 独立测试：
   - 模块仓内有完整的单元测试
   - 装配仓有集成测试（模块组合测试）
   - 发版前跑全量回归

3. 影响面分析（AI 辅助）：
   // 修改 sku 模块 -> AI 自动分析
   // 哪些模块订阅了 sku_change 事件？
   // 哪些模块依赖 skuStore 的字段？
   // 生成影响面报告

4. 灰度隔离：
   - 模块级灰度：只对修改的模块灰度
   - 监控模块维度的 Error 率
   - 异常自动回滚单个模块版本

5. 版本管理：
   - semver 严格执行
   - breaking change 必须 major 版本号
   - 装配仓可以锁定模块版本不升级

实际案例：
修改 SKU 模块的选择逻辑 -> 影响面分析发现 price/bottomBar/time 模块有依赖
-> 在这些模块都加了防御（可选链 + fallback）
-> 灰度验证无异常后全量`,
    oralAnswer: `保证模块修改不影响其他模块，我从架构和工程两个层面做保障。

架构层面：每个模块有明确的输入 Props 和输出 Events，内部状态不暴露。模块间不直接 import 而是通过事件或 Store 通信。每个模块有独立的 MobX Store。TypeScript interface 严格定义模块 Props，变化必须向前兼容。

工程层面有五个保障。第一模块级 ErrorBoundary，模块 A 报错不会导致其他模块白屏。第二模块仓内有完整单测，装配仓有集成测试。第三 AI 辅助影响面分析——修改 SKU 模块时自动分析哪些模块订阅了 sku_change 事件、哪些依赖 skuStore 字段，生成影响面报告。第四模块级灰度，只对修改的模块灰度监控，异常可回滚单个模块。第五 semver 严格执行，breaking change 必须 major 版本号。

实际案例：修改 SKU 选择逻辑，影响面分析发现 price/bottomBar/time 有依赖，这些模块都加了防御后灰度验证通过再全量。`,
  },
  {
    id: 4009,
    category: '二面深度',
    difficulty: 'hard',
    question: '如果让你优化一个 10s 才能加载完的 RN 页面，你的排查思路是什么？如何系统性地定位瓶颈？',
    answer: `系统性性能排查思路：

第一步：分段计时，定位大头
T0: 用户点击
T1: Native 容器创建完成 (T1-T0: 容器耗时)
T2: Bundle 加载完成 (T2-T1: Bundle 耗时)
T3: JS 首次执行完成 (T3-T2: JS 初始化)
T4: BFF 数据返回 (T4-T3: 网络耗时)
T5: 首屏渲染完成 (T5-T4: 渲染耗时)

找到最大的 gap 重点优化。

第二步：各阶段排查清单

Bundle 耗时过长（T2-T1 > 2s）：
- Bundle 体积多大？是否该拆分？
- 是否预加载了？
- 是否有不必要的依赖被打包？

JS 初始化慢（T3-T2 > 1s）：
- 组件树太深？
- 有同步的重计算？（require 时执行的代码）
- 全局 import 太多？

网络慢（T4-T3 > 2s）：
- BFF 接口 RT 多少？后端哪里慢？
- 是否可以预请求/缓存？
- 数据量是否过大？需要裁剪？

渲染慢（T5-T4 > 2s）：
- 一次性渲染多少模块？
- 有没有模块优先级？
- 图片数量/尺寸是否合理？
- 是否有不必要的 re-render？

第三步：工具辅助
- React DevTools Profiler: 找到慢的组件
- Flipper: 网络请求/日志/布局检查
- Systrace (Android): Native 层帧率分析
- Instruments (iOS): CPU/Memory/GPU 分析
- 自建性能打点: 各节点耗时上报

第四步：优化优先级（ROI排序）
1. 收益大 + 成本低：预请求、缓存、图片裁剪
2. 收益大 + 成本中：Bundle拆分、模块优先级
3. 收益大 + 成本高：架构重构、MRNBox快照
4. 收益小：先不做，记录 backlog`,
    oralAnswer: `排查 10 秒加载的页面我会系统性分段定位。

第一步分段计时找大头：用户点击到容器创建是多少，容器创建到 Bundle 加载完是多少，Bundle 加载到 JS 执行完是多少，JS 执行到 BFF 数据回来是多少，数据回来到首屏渲染完是多少。找到最大的 gap 重点优化。

第二步针对性排查。Bundle 耗时长看体积是不是该拆分、有没有预加载、有没有不必要的依赖。JS 初始化慢看组件树是不是太深、有没有同步重计算。网络慢看 BFF RT 多少、能不能预请求或缓存、数据量能不能裁剪。渲染慢看一次渲染多少模块、有没有做优先级、有没有不必要的 re-render。

第三步用工具辅助：React DevTools Profiler 找慢组件，Flipper 看网络请求，Systrace 分析 Native 帧率，自建性能打点上报各节点耗时。

第四步按 ROI 排优先级：收益大成本低的先做比如预请求缓存图片裁剪，收益大成本高的如架构重构排后面。`,
  },
  {
    id: 4010,
    category: '二面深度',
    difficulty: 'hard',
    question: '你觉得 SDUI 和 DSL 方案相比，各有什么优劣？什么场景用 SDUI 更合适？',
    answer: `SDUI vs DSL 对比：

SDUI（Server Driven UI）：
- 服务端返回「数据 + 结构描述」
- 前端有预注册的组件库
- 服务端选择组件并传递数据
- 例：美团详情页、淘宝首页

DSL（Domain Specific Language）：
- 服务端返回「完整的 UI 描述」（类似 JSON 描述的 UI 树）
- 前端按 DSL 动态创建 UI
- 更灵活但也更复杂
- 例：Flutter 动态化、阿里 DinamicX

对比分析：
| 维度 | SDUI | DSL |
|------|------|-----|
| 灵活性 | 中（受限于已注册组件）| 高（任意UI）|
| 性能 | 好（原生组件）| 中（动态创建有开销）|
| 安全性 | 高（前端控制渲染逻辑）| 中（需要沙箱）|
| 开发成本 | 低（组件化思维）| 高（需要 DSL 引擎）|
| 调试难度 | 低（组件可单独调试）| 高（动态生成难追踪）|
| 包大小 | 小（不需要DSL引擎）| 大（需要解析+渲染引擎）|
| 跨端复用 | 组件各端实现 | DSL 引擎各端实现 |

SDUI 更适合的场景：
1. 页面结构相对稳定（详情页、商品页）
2. 模块种类有限（几十种组件足够覆盖）
3. 需要高性能（原生组件渲染）
4. 多团队协作（模块粒度分工）

DSL 更适合的场景：
1. 运营活动页（频繁变化、千人千面）
2. 低代码平台（用户自定义 UI）
3. 极致动态化需求

美团选择 SDUI 的原因：
- 详情页模块数量有限（20-30种）
- 性能要求高（亿级 PV）
- 团队协作需要模块化分工
- 不需要任意 UI 的灵活度`,
    oralAnswer: `SDUI 和 DSL 的核心区别是：SDUI 服务端返回数据加结构描述，前端有预注册的组件库来渲染。DSL 服务端返回完整的 UI 描述，前端按 DSL 动态创建 UI，更灵活但也更复杂。

SDUI 优势是性能好因为用原生组件、安全性高因为前端控制渲染逻辑、开发成本低用组件化思维、不需要 DSL 引擎包也小。劣势是灵活性受限于已注册的组件。

DSL 优势是任意 UI 极其灵活，劣势是动态创建有性能开销、需要沙箱保证安全、需要解析加渲染引擎包体积大、调试困难。

SDUI 适合页面结构相对稳定、模块种类有限、需要高性能、多团队协作的场景。DSL 适合运营活动页频繁变化、低代码平台、极致动态化需求。

美团选 SDUI 因为详情页模块就 20 到 30 种足够覆盖，亿级 PV 性能要求高，团队需要模块化分工，不需要任意 UI 的灵活度。`,
  },
  {
    id: 4011,
    category: '二面深度',
    difficulty: 'hard',
    question: '从工程管理角度，20+ 个模块由不同人开发，如何保证代码质量和风格一致性？',
    answer: `代码质量治理体系：

1. 规范层：
   - ESLint + Prettier 统一配置（公共能力仓提供）
   - TypeScript strict 模式
   - 命名规范文档（组件/变量/文件/目录）
   - Git Commit Message 规范（conventional commits）

2. 模板层：
   - 模块脚手架：创建新模块自动生成标准目录结构
   - 标准模板包含：index.ts / store.ts / types.ts / __tests__/
   - 样式规范：统一 design token（颜色/间距/字号）

3. Review 层：
   - 必须 2 人 Review 通过才能合并
   - Review Checklist：
     * 类型安全（无 any/as）
     * 错误处理（可选链/ErrorBoundary）
     * 性能（无不必要的 re-render）
     * 埋点完整性
     * 无 console.log 残留

4. 自动化层：
   - CI 门禁：lint + test + build 全通过
   - 覆盖率要求：新代码 >= 80%
   - Bundle Size 检查：超出阈值需说明原因
   - 依赖安全扫描

5. 知识共享层：
   - 周会 Code Review 分享
   - 公共文档：最佳实践 + 踩坑记录
   - AI rules 沉淀：将规范转化为 AI 可理解的规则

实际效果：
- 新人上手时间从 2 周缩短到 3 天
- 线上 Bug 率下降 40%
- 代码风格一致性评分 > 95%`,
    oralAnswer: `20 多个模块不同人开发保证质量一致性，我建了五层治理体系。

规范层：ESLint 加 Prettier 统一配置由公共能力仓提供，TypeScript strict 模式，命名规范文档，Git Commit 用 conventional commits。

模板层：模块脚手架创建新模块自动生成标准目录结构——index.ts、store.ts、types.ts、__tests__/，样式统一用 design token。

Review 层：必须 2 人 Review 通过才能合并，Checklist 检查类型安全无 any、错误处理有可选链和 ErrorBoundary、性能无不必要 re-render、埋点完整性、无 console.log 残留。

自动化层：CI 门禁 lint 加 test 加 build 全通过，新代码覆盖率要求 80% 以上，Bundle Size 超阈值要说明原因。

知识共享层：周会 Code Review 分享，公共文档记录最佳实践和踩坑，AI rules 沉淀把规范转化为 AI 可理解的规则。效果是新人 3 天上手，线上 Bug 率降 40%。`,
  },
  {
    id: 4012,
    category: '二面深度',
    difficulty: 'hard',
    question: '你怎么看待 React Native 的未来？如果现在选型，你会选 RN 还是 Flutter？为什么？',
    answer: `RN vs Flutter 选型分析：

React Native 优势：
1. JS 生态：npm 百万包直接可用
2. 热更新：合规支持 OTA 更新（国内刚需）
3. 团队复用：前端工程师无学习成本
4. Web 技术栈：和 H5 共享逻辑代码
5. 新架构成熟：JSI/Fabric/TurboModules 性能大幅提升
6. 渐进式接入：可以和 Native 混合开发

Flutter 优势：
1. 渲染一致性：自绘引擎，各平台像素级一致
2. 性能上限：无 Bridge 开销，接近原生
3. Dart 单线程+AOT：启动快、运行稳
4. 开发效率：Hot Reload 极快
5. 桌面端：同时支持 Windows/macOS/Linux

选型考虑因素：

选 RN 的场景：
- 已有前端团队，不想招 Dart 工程师
- 需要热更新能力（App Store 审核慢）
- 需要和 Web 共享业务逻辑
- 渐进式改造：已有 Native App 部分页面 RN 化
- 美团就是这个策略：MRN 体系成熟

选 Flutter 的场景：
- 新项目从零开始，团队可以重组
- 对 UI 一致性要求极高（品牌设计）
- 不需要热更新
- 性能要求极致（游戏化/复杂动画）
- 跨平台扩展到桌面

我的判断：
- 国内大厂短期仍以 RN 为主（热更新+团队惯性）
- Flutter 在新兴项目和中小公司增长快
- 长期两者会共存，各有适合的场景
- 关键不是选哪个框架，而是选适合团队和业务的方案`,
    oralAnswer: `RN 和 Flutter 各有优势我分场景来说。

RN 优势：JS 生态 npm 百万包可用，合规支持热更新这在国内是刚需，前端团队零学习成本，和 H5 能共享逻辑代码，新架构 JSI/Fabric 性能大幅提升，可以渐进式接入和 Native 混合开发。

Flutter 优势：自绘引擎各平台像素级一致，无 Bridge 性能上限接近原生，Dart AOT 启动快运行稳，Hot Reload 极快，还能同时支持桌面端。

选 RN 的场景：已有前端团队、需要热更新、需要和 Web 共享逻辑、渐进式改造已有 Native App。美团就是这个策略，MRN 体系很成熟。

选 Flutter 的场景：新项目从零开始团队可重组、对 UI 一致性要求极高、不需要热更新、性能要求极致、要扩展到桌面端。

我的判断是国内大厂短期仍以 RN 为主因为热更新和团队惯性，Flutter 在新兴项目增长快，长期两者共存。关键不是选哪个框架而是选适合团队和业务的方案。`,
  },
  {
    id: 4013,
    category: '二面深度',
    difficulty: 'hard',
    question: '你在 AI Coding 实践中，如何衡量 AI 真的提升了效率？有没有量化数据？',
    answer: `AI Coding 效率量化方案：

衡量维度：

1. 开发速度：
   - 需求交付周期：从需求评审到上线的天数
   - 编码时间：纯编码阶段的工时
   - 对比基准：相同复杂度的需求，AI辅助 vs 纯手工

2. 代码质量：
   - Bug 率：AI 辅助开发的模块 vs 手工开发的模块
   - CR 打回次数：AI 代码的 Review 修改轮次
   - 线上问题数：上线后的 hotfix 次数

3. 特定场景效率：
   - MRN → 小程序迁移：
     * 手工迁移一个模块：2-3 天
     * AI 辅助迁移：0.5-1 天
     * 效率提升：60%+

   - 影响面分析：
     * 手工看代码依赖：2-4 小时
     * AI 自动分析：5-10 分钟
     * 效率提升：90%+

   - 模块开发：
     * 手工从零写一个标准模块：2-3 天
     * AI 生成框架 + 人工调整：0.5-1 天
     * 效率提升：50-60%

4. 不适合的场景（效率可能降低）：
   - 高度创新性的架构设计（AI 没有参考）
   - 极度个性化的业务逻辑（AI 理解困难）
   - 调试已有 Bug（AI 可能引入新问题）

量化方法：
- 同一个人做相似需求，AB 对比
- 记录每个模块的开发工时和 AI 参与度
- 跟踪 AI 生成代码的线上质量指标

总结：AI 在「模式明确+规则清晰」的任务上提效最大，
在「创造性+模糊性」的任务上提效有限。`,
    oralAnswer: `衡量 AI 提效我从三个维度量化。

开发速度维度：同一个人做相似复杂度需求，对比 AI 辅助和纯手工的交付周期和编码工时。代码质量维度：对比 AI 辅助模块和手工模块的 Bug 率、CR 打回次数和上线后 hotfix 次数。

具体场景数据：MRN 到小程序迁移，手工迁移一个模块 2 到 3 天，AI 辅助 0.5 到 1 天，提效 60% 以上。影响面分析，手工看代码依赖 2 到 4 小时，AI 自动分析 5 到 10 分钟，提效 90% 以上。模块开发，手工从零写 2 到 3 天，AI 生成框架加人工调整 0.5 到 1 天，提效 50% 到 60%。

但也有不适合的场景：高度创新性的架构设计 AI 没有参考，极度个性化的业务逻辑 AI 理解困难，调试已有 Bug AI 可能引入新问题。

总结是 AI 在模式明确加规则清晰的任务上提效最大，在创造性加模糊性的任务上提效有限。`,
  },
  {
    id: 4014,
    category: '二面深度',
    difficulty: 'hard',
    question: '你做的 Snapshot 交易快照页是什么？为什么需要单独做一个快照页而不复用详情页？',
    answer: `Snapshot 交易快照页：

定义：
用户下单后，保存当时商品详情的"快照"，用于：
- 历史订单查看：购买了什么、当时什么价格
- 交易纠纷：证明下单时的商品描述/价格
- 法律合规：交易凭证保留

为什么不复用详情页：

1. 数据来源不同：
   - 详情页：实时请求 BFF，获取当前最新数据
   - 快照页：从订单系统读取下单时的冻结数据
   - 商品可能已下架/改价/改图，但快照要保持原样

2. 交互逻辑不同：
   - 详情页：可以加购、切换 SKU、发起聊天
   - 快照页：只读展示，交互大幅收敛
   - 不需要 BottomBar 的购买按钮
   - 不需要门店跳转（门店可能已关闭）

3. 模块裁剪：
   - 快照页只保留展示类模块（头图、价格、结构化详情）
   - 移除动态模块（推荐、评价、问答）
   - 移除交互模块（SKU选择器、可约时间）

4. 稳定性要求更高：
   - 快照页不能因为模块报错而白屏
   - 降级策略更保守：模块报错直接隐藏

实现方式：
- 复用 SDUI 模块化体系
- BFF 返回快照专用的 layout（模块更少）
- 模块组件复用详情页的（只是数据源不同）
- 额外处理：已下架标识、历史价格标注

技术挑战：
- 数据格式兼容：历史订单的数据结构可能是旧版
- 图片资源：快照图片需要永久存储（CDN 不能过期）
- 模块兼容：新版模块要能渲染旧版数据`,
    oralAnswer: `Snapshot 交易快照页是用户下单后保存当时商品详情的"冻结"，用于历史订单查看、交易纠纷举证和法律合规凭证。

不复用详情页的原因有四个。第一数据来源不同：详情页实时请求 BFF 获取最新数据，快照页从订单系统读取下单时冻结的数据，商品可能已下架改价但快照要保持原样。第二交互逻辑不同：详情页可以加购切 SKU，快照页只读展示不需要购买按钮。第三模块裁剪：快照页只保留展示类模块如头图价格，移除动态模块如推荐评价和交互模块如 SKU 选择器。第四稳定性要求更高：快照页不能白屏，模块报错直接隐藏。

实现方式是复用 SDUI 模块化体系，BFF 返回快照专用的 layout 模块更少，组件复用详情页的只是数据源不同。技术挑战是历史订单数据结构可能是旧版格式要兼容，快照图片要永久存储 CDN 不能过期，新版模块要能渲染旧版数据。`,
  },
  {
    id: 4015,
    category: '二面深度',
    difficulty: 'hard',
    question: '站在 P6-P7 的角度，你觉得一个高级前端工程师和普通前端最大的区别是什么？技术之外你关注什么？',
    answer: `P6-P7 的核心差异：

技术维度：
1. 系统性思维 vs 点状解题
   - 初级：遇到 Bug 解 Bug，写功能就写功能
   - 高级：思考为什么会有这个 Bug？如何系统性预防？
   - 例：不只是修一个空指针，而是建立数据防御层

2. 架构设计能力
   - 初级：能实现需求
   - 高级：能设计方案让团队高效实现需求
   - 例：SDUI + 多仓架构让 10 人团队并行开发

3. 技术判断力
   - 初级：用什么技术都可以
   - 高级：知道什么场景用什么方案，能说清 trade-off
   - 例：选 MobX 还是 Zustand？缓存策略怎么定？

技术之外关注的：

1. 业务理解：
   - 详情页的核心指标是什么？（转化率）
   - 首屏优化为什么重要？（每快 100ms 转化率提升 X%）
   - 技术方案要和业务目标对齐

2. 团队协作：
   - 如何让其他人能高效使用我设计的架构？
   - 文档、规范、脚手架是否完善？
   - 技术方案是否考虑了团队现状？

3. 影响力：
   - 不只是完成自己的模块，还能影响团队方向
   - 沉淀可复用的方案（MRNBox 方案可推广到其他页面）
   - 推动规范落地（AI Coding 体系建设）

4. 成长性：
   - 持续关注技术趋势（新架构/AI/跨端）
   - 不满足于「能用就行」，追求更优解
   - 能从项目中提炼通用方法论

我的理解：
P6 = 能独立负责一个完整的技术方向
P7 = 能定义方向并带动团队一起做好`,
    oralAnswer: `高级和普通前端最大的区别在三个技术维度。

第一系统性思维：初级遇到 Bug 解 Bug，高级思考为什么会有这个 Bug 如何系统性预防。比如不只修一个空指针而是建立数据防御层。第二架构设计能力：初级能实现需求，高级能设计方案让团队高效实现需求，比如 SDUI 加多仓架构让 10 人并行开发。第三技术判断力：知道什么场景用什么方案能说清 trade-off。

技术之外我关注四个方面。业务理解——详情页核心指标是转化率，首屏快 100ms 转化率提升多少，技术方案要和业务目标对齐。团队协作——我设计的架构别人能不能高效使用，文档规范脚手架是否完善。影响力——不只完成自己的模块还能影响团队方向，沉淀可复用的方案。成长性——持续关注技术趋势，从项目中提炼通用方法论。

我的理解是 P6 能独立负责一个完整技术方向，P7 能定义方向并带动团队一起做好。`,
  },
  {
    id: 4016,
    category: '二面深度',
    difficulty: 'medium',
    question: '你在项目中是怎么做灰度发布的？灰度过程中出了问题怎么快速回滚？',
    answer: `灰度发布策略：

灰度维度：
1. 用户百分比：5% -> 20% -> 50% -> 100%
2. 设备类型：先 iOS 再 Android（或反过来）
3. 地域：先小城市再大城市
4. 用户群：先内部员工再普通用户

灰度流程（MRN 热更新）：
1. 发布 Bundle 到 PushY，设置灰度比例 5%
2. 观察 1-2 小时：
   - JS Error 率是否上升？
   - 页面加载时间是否劣化？
   - 业务指标（PV/UV/转化率）是否异常？
3. 无异常 -> 扩大到 20%
4. 继续观察 -> 50% -> 100%

AB 实验灰度（SDUI 场景）：
- BFF 侧配置实验：对照组 vs 实验组
- 通过 userId 哈希分桶
- 实时监控两组的核心指标差异
- 统计显著后全量

快速回滚机制：

1. 热更新回滚：
   - PushY 后台一键回滚到上一版本
   - 用户下次启动自动加载旧 Bundle
   - 紧急情况：强制更新到旧版

2. BFF 配置回滚：
   - 关闭实验 -> 所有用户回到对照组
   - 恢复上一版 layout 配置
   - 秒级生效

3. 模块级回滚：
   - 装配仓 lockfile 中锁定模块版本
   - 回退特定模块到上一版本
   - 不影响其他模块

自动回滚触发条件：
- JS Error 率突增 > 3 倍
- 白屏率 > 2%
- P0 告警触发后 5 分钟无人响应

经验：宁可多灰度一天，不要急于全量。`,
    oralAnswer: `灰度发布我们从四个维度控制：用户百分比 5% 到 20% 到 50% 到 100%，设备类型先 iOS 后 Android，地域先小城市后大城市，用户群先内部员工后普通用户。

MRN 热更新灰度流程：发布 Bundle 到 PushY 设 5% 灰度，观察 1 到 2 小时看 JS Error 率、加载时间和业务指标是否异常，无异常扩到 20%，继续观察再 50% 最后全量。

快速回滚有三种机制。热更新回滚：PushY 后台一键回滚上一版本，用户下次启动自动加载旧 Bundle。BFF 配置回滚：关闭实验所有用户回对照组秒级生效。模块级回滚：装配仓 lockfile 锁定模块版本回退单个模块不影响其他。

自动回滚触发条件：JS Error 率突增 3 倍、白屏率超 2%、P0 告警 5 分钟无人响应自动触发。经验是宁可多灰度一天不要急于全量。`,
  },
  {
    id: 4017,
    category: '二面深度',
    difficulty: 'medium',
    question: '你提到处理小程序 Skyline 和 MRN 的首屏一致性治理，具体怎么做的？怎么验证一致性？',
    answer: `首屏一致性治理：

目标：用户在 MRN 和小程序看到的首屏视觉效果一致。

一致性问题来源：
1. 布局差异：Flexbox 实现细节不同
2. 字体差异：系统字体大小/行高不同
3. 图片差异：裁剪方式/比例处理不同
4. 间距差异：dp vs rpx 转换精度
5. 颜色差异：色彩空间处理不同

治理流程：

1. 基准定义：
   - 以 MRN iOS 高端机为视觉基准
   - 设计稿标注作为终极标准
   - 允许 2px 以内偏差

2. 对比方式：
   a) 人工对比：双端截图左右放置
   b) 像素对比：自动截图 + 图片 diff 工具
   c) 关键指标对比：模块高度/宽度/间距提取

3. 自动化验证：
   // 每次小程序模块修改后，自动跑对比
   - MRN 端截图（基准）
   - 小程序端截图（待验证）
   - 像素 diff：相似度 < 95% 则标记需要修复

4. 常见修复手段：
   - rpx 计算精度：用 Math.round 避免小数
   - 行高对齐：明确设置 line-height 而非依赖默认值
   - 图片裁剪：封装统一裁剪逻辑
   - 字重映射：不同平台的 fontWeight 值映射

5. 治理工具：
   - 对比看板：列出所有模块的一致性评分
   - 问题跟踪：diff 标红的区域生成修复任务
   - 回归检查：每次发版前跑全量对比

效果：
- 首屏一致性从最初 70% 提升到 95%+
- 核心模块（头图/价格/SKU）100% 对齐`,
    oralAnswer: `首屏一致性治理的目标是用户在 MRN 和小程序看到的首屏视觉效果一致。

不一致的来源有五个：Flexbox 实现细节不同、系统字体大小行高不同、图片裁剪方式不同、dp 和 rpx 转换精度不同、色彩空间处理不同。

治理流程分五步。第一定基准：以 MRN iOS 高端机为视觉基准，设计稿为终极标准，允许 2px 以内偏差。第二对比方式：人工双端截图左右放置，自动化像素 diff 工具，关键指标如模块高宽间距提取对比。第三自动化验证：每次小程序模块修改后自动跑截图 diff，相似度低于 95% 标记修复。第四常见修复：rpx 计算用 Math.round 避免小数，明确设置 line-height，封装统一图片裁剪逻辑，fontWeight 分平台映射。第五治理工具：对比看板列出所有模块一致性评分，diff 标红区域生成修复任务，发版前全量对比回归。

效果是一致性从 70% 提升到 95% 以上，核心模块 100% 对齐。`,
  },
  {
    id: 4018,
    category: '二面深度',
    difficulty: 'medium',
    question: '你在建盛材做了 NFC "碰一碰"获客功能，技术上是怎么实现的？',
    answer: `NFC "碰一碰"获客技术实现：

业务场景：
线下建材门店在柜台放置 NFC 标签
顾客手机碰一碰 -> 自动打开营销页面 -> 领取优惠/关注店铺

技术方案：

1. NFC 标签写入：
   - 标签类型：NDEF（NFC Data Exchange Format）
   - 写入内容：URL（营销落地页链接 + 门店ID + 渠道标识）
   - 格式：https://jsc.xxx.com/landing?shopId=123&channel=nfc

2. 手机端触发：
   - iOS：系统自动识别 NDEF URL，弹出 Safari 打开
   - Android：系统自动识别，跳转默认浏览器或 App（如已安装）
   - 无需额外 App 或设置

3. 落地页实现（H5）：
   - URL 参数解析：shopId、channel、timestamp
   - 根据 shopId 展示对应门店的营销内容
   - 渠道归因：标记为 NFC 获客
   - 引导操作：领券/关注/注册

4. 数据追踪：
   - 每次碰触记录：时间、设备类型、地理位置
   - 转化漏斗：碰触 -> 打开页面 -> 领券 -> 到店消费
   - 渠道 ROI：NFC 获客成本 vs 其他渠道

5. RN App 中的配合：
   - App 内 NFC 读取（用于店员配置标签）
   - react-native-nfc-manager 库
   - 写入/读取/格式化 NFC 标签

技术注意事项：
- NFC 标签存储空间有限（通常 < 1KB）
- URL 要尽量短（用短链服务）
- 考虑标签损坏/被覆盖的情况
- iOS 只支持读取，不支持后台监听`,
    oralAnswer: `NFC 碰一碰获客的技术实现分五部分。

NFC 标签写入：标签用 NDEF 格式，写入营销落地页 URL 加门店 ID 和渠道标识。手机端触发：iOS 系统自动识别 NDEF URL 弹出 Safari 打开，Android 跳转默认浏览器或已安装的 App，用户不需要额外操作。

落地页是 H5 实现：解析 URL 参数获取 shopId 和 channel，根据 shopId 展示对应门店营销内容，标记为 NFC 渠道获客，引导用户领券关注注册。

数据追踪：每次碰触记录时间、设备类型、地理位置。转化漏斗是碰触到打开页面到领券到到店消费。可以算出 NFC 渠道的获客 ROI。

RN App 中配合：用 react-native-nfc-manager 库让店员在 App 内配置和写入 NFC 标签。注意事项是标签存储空间小于 1KB 所以 URL 要用短链服务，iOS 只支持读取不支持后台监听。`,
  },
  {
    id: 4019,
    category: '二面深度',
    difficulty: 'hard',
    question: '如果详情页的 BFF 接口突然变慢了（RT 从 200ms 涨到 2s），你怎么排查和应对？',
    answer: `BFF 接口变慢排查和应对：

即时应对（5分钟内）：
1. 确认影响范围：全部用户 or 部分？全部商品 or 特定品类？
2. 看 BFF 服务自身：CPU/内存/GC 是否异常？
3. 如果是全量：考虑启用降级方案（读缓存）

排查思路（分层定位）：

1. 网络层：
   - CDN -> BFF 的链路是否正常？
   - DNS 解析是否变慢？
   - 是否有网络抖动/丢包？

2. BFF 服务层：
   - 服务实例是否正常？有没有被打满？
   - 最近有没有发版/配置变更？
   - GC 是否频繁？（Java/Node 都可能）
   - 连接池是否耗尽？

3. 下游服务层：
   - BFF 调用的后端微服务哪个慢了？
   - 数据库查询是否有慢 SQL？
   - 缓存是否失效（缓存穿透）？
   - 第三方服务是否超时？

4. 数据层：
   - 是否有大促导致流量暴增？
   - 是否有批量写入导致读变慢？

排查工具：
- 链路追踪（requestId 追踪全链路耗时）
- 日志系统（关键节点打点）
- 监控大盘（QPS/RT/错误率趋势）

前端应对策略：

1. 超时兜底：
   - BFF 请求设 3s 超时
   - 超时后展示缓存数据 + "信息可能不是最新"提示

2. 缓存降级：
   - firstScreenBffCache 价值凸显
   - 缓存数据直接展示，后台慢慢刷新

3. 骨架屏优化：
   - 接口慢时用户至少看到骨架屏而非白屏
   - 加进度提示"加载中..."

4. 局部渲染：
   - 部分模块数据先到先渲染
   - 不等所有数据聚合完成

5. 重试策略：
   - 首次超时后自动重试一次（间隔 1s）
   - 两次都失败才展示错误态`,
    oralAnswer: `BFF 接口突然变慢我分即时应对和系统排查两部分处理。

即时应对 5 分钟内：确认是全部用户还是部分，看 BFF 服务 CPU 内存 GC 是否异常，如果全量就启用降级方案读缓存。

系统排查分层定位：网络层看 CDN 到 BFF 链路是否正常、DNS 是否变慢。BFF 服务层看实例是否被打满、最近有没有发版、连接池是否耗尽。下游服务层看调用的哪个微服务慢了、有没有慢 SQL、缓存是否穿透。数据层看是不是大促流量暴增。工具用链路追踪 requestId 追全链路耗时，日志系统看关键节点打点。

前端应对策略五个：BFF 请求设 3 秒超时超时后展示缓存数据。firstScreenBffCache 缓存降级。接口慢时骨架屏加进度提示不白屏。部分模块数据先到先渲染不等聚合完成。首次超时自动重试一次间隔 1 秒，两次失败才展示错误态。`,
  },
  {
    id: 4020,
    category: '二面深度',
    difficulty: 'hard',
    question: '从你的职业规划看，你觉得接下来 1-2 年应该重点提升什么能力？你对 AI 时代前端工程师的定位怎么看？',
    answer: `职业发展思考：

1-2 年重点提升方向：

1. 架构设计能力：
   - 从「实现者」到「设计者」
   - 能独立设计一个完整的技术方案
   - 能评估方案的长期可维护性

2. 技术深度：
   - RN 新架构（Fabric/TurboModules）原理级理解
   - 跨端渲染引擎原理
   - 性能优化从「经验驱动」到「数据驱动」

3. 技术影响力：
   - 技术分享和沉淀
   - 从解决自己的问题到解决团队的问题
   - 方案的可复制性和可推广性

AI 时代前端工程师的定位：

我的判断：AI 不会替代前端，但会重塑工作方式。

变化：
- 重复性编码工作 AI 可以完成（模板代码、CRUD、迁移）
- 但架构设计、业务理解、技术判断仍需人
- 「会用 AI」变成基本能力而非竞争优势

前端工程师的新定位：
1. AI 协作者：不是手写每行代码，而是设计规则让 AI 高效产出
2. 架构师：定义系统的骨架，AI 填充血肉
3. 质量守护者：审查 AI 输出，确保正确性
4. 业务翻译者：将业务需求转化为 AI 可理解的 Spec

具体到我的实践：
- AGENTS/rules/knowledge 就是在「教 AI 理解项目」
- 未来更多工作是「设计规则」而非「写代码」
- 但理解底层原理仍然是设计好规则的前提

我的优势：
- 有实际 AI Coding 工程化落地经验
- 理解 AI 的能力边界（什么能做什么不能做）
- 能持续优化人机协作流程`,
    oralAnswer: `1 到 2 年我重点提升三个方向。

架构设计能力：从实现者到设计者，能独立设计完整技术方案并评估长期可维护性。技术深度：RN 新架构 Fabric/TurboModules 原理级理解，跨端渲染引擎原理，性能优化从经验驱动到数据驱动。技术影响力：从解决自己的问题到解决团队的问题，方案可复制可推广。

对 AI 时代前端工程师的看法：AI 不会替代前端但会重塑工作方式。重复性编码 AI 能做但架构设计、业务理解、技术判断仍需人。"会用 AI"变成基本能力而非竞争优势。

前端工程师的新定位有四个：AI 协作者——设计规则让 AI 高效产出而不是手写每行代码。架构师——定义系统骨架 AI 填充血肉。质量守护者——审查 AI 输出确保正确性。业务翻译者——将需求转化为 AI 可理解的 Spec。

我的实践就是 AGENTS/rules/knowledge 在"教 AI 理解项目"，未来更多工作是设计规则而非写代码，但理解底层原理仍然是设计好规则的前提。`,
  },
];
